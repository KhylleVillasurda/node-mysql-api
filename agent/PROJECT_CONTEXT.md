# CONTEXT

We are building a deployable website using angular on the frontend with pnpm and the backend using node.js and mysql.
Vercel in the frontend and Railway in the backend, both hosting platforms are free to use.
The codebase must align for the compatability with these hosting sites to avoid future complexity and compatibility issues.
